dns_ip_addr: 10.20.112.10
cluster_name: "k8s.test"
dns_domain: "{{ cluster_name }}"
kube_dir: /etc/kubernetes
coredns_version: 1.6.2
corends_url_binary: https://github.com/coredns/coredns/releases/download/v{{ corends_version }}/coredns_{{ corends_version }}_linux_amd64.tgz

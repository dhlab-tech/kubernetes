upstream masters {
{% for host in master_hosts %}
         server {{ host }};
{% endfor %}
}

server {
        listen 6443;
        server_name {{ ha_master_hostname }};

        location / {
                proxy_pass https://masters;
        }
}

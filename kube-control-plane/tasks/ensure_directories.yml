---
- name: Ensure tmp dir
  file:
    path: "/var/tmp"
    state: directory
- name: Ensure k8s dir
  file:
    path: "{{ kube_dir }}"
    state: directory
- name: Ensure user systemd 
  file:
    path: "/usr/lib/systemd/system"
    state: directory
